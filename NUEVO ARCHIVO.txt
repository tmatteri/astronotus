ESTE ARCHIVO FUE SUBIDO DESDE CONSOLA

PASOS

1 ) HACER QUE LA CARPETA DEL PROYECTO SEA .Git (Git init)

2 ) Agregar un archivo al proyecto (git add "ArchivoNuevo.txt")

3 ) Hacer commit (genera la version local) (git commit -m "Esto es el comentario del commit")

4 ) Hacer el push al github (git remote push astronotus) 